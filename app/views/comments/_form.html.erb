<%= form_for @comment do |f| %>
<%= f.label :name %> <%= f.text_field :name %><br>
<%= f.label :body %> <%= f.text_area :body %><br>
<%= f.label :recipe_id, "The Recipe" %> <br>
<%= f.collection_select :recipe_id, Recipe.all,:id, :title  %>

<%= f.submit %>
<% end %>