<%= form_for @recipe.comments.new do |f| %>
<%= f.label :name %> <br> <%= f.text_field :name %><br>
<%= f.label :body, "Comment" %> <br> <%= f.text_area :body %><br>
<%= hidden_field_tag "comment[recipe_id]", params[:id] %>
<%= f.submit %>
<% end %>